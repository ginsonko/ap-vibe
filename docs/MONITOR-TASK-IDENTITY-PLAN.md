# 任务监看身份与显示修正

2026-09-10浏览器实测：七天图表把28个标签位等分，日期变为省略号。当前主任务和媒体任务同一稳定session_id在多个历史project来源下出现重复卡片。认知配置尚未读取时按钮已经可点击，会用空默认值打开编辑。

监看是全机只读投影：同一Codex session_id显示一张任务卡；没有ID时仍按source_key隔离，不按标题合并。记录完整来源项目映射，优先采用已确认归属的代表来源；多个已确认项目保留列表并标明，选最新代表供查看。合并消息按角色/时间/文本去重，计数在截断到80条显示前计算。限定项目查询仍只含该项目来源，底层登记、归类、档案和旧活动不移动。活动点击按来源项目和source_key关联，不能丢历史入口。

图表只展示少量完整时间刻度并对齐首尾，日期模式使用短日期；值仍来自真实点。认知页在配置读取完成后才开放编辑，读失败保留错误与原配置。会话详情使用命名section，消除嵌套main语义。

审查：跨project同名但不同ID不得合并；相同ID保留所有provenance；不能用合并后的bounded消息数代替已读总数；相同内容但不同时间保留；来源错误和未知不能删除。全局聚合不更改写入身份。新增纯投影边界测试与真实主任务重复样本复查；浏览器检查完整标签、当前标题/历史点击和无白屏，未完成托管任务期间不重启服务。
